	<% if @is_delete == true %>
		var row = $('.delete_<%= @department.id %>').closest("tr").get(0);
		$(row).fadeTo(400, 0, function () { 
			$(row).remove();
		});
	<% else %>
		alert("<%= @message %>");
	<% end %>
	